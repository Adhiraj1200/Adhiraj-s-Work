base = float(input('Enter the base of the base of a triangle:'))
height = float(input('Enter the height of the base of a triangle:'))
area = height * base * 0.5
print('The area of the triangle is:',area)